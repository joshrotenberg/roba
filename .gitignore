/target

# maintainer-private agent context (auto-loaded locally, never tracked)
CLAUDE.local.md

# local design/working docs (never tracked; protects against `git add -A`)
structured-tasks-overview.md
